webscreenshot
==================

## Installation

```
$ git@github.com:jnreynoso/webscreenshot.git
```
### Pre-requisites

    # Python 2.7
    # Invoke
    
### Install Invoke

    # pip install invoke
   
## Usage

`Invoke`
```
$ invoke install
$ invoke runserver
$ invoke install runserver
```
